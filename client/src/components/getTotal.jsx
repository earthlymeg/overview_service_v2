import React from 'react';

const getTotal = ({ bag }) => {
  let total = 0;

  console.log({bag});

  // for (let i = 0; i < bag.length; i++) {
  //   total += i.price;
  // }

  return total;
}

export default getTotal;